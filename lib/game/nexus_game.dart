import 'package:flame/game.dart';

/// The main Flame game engine instance for Nexus Mortis.
///
/// Responsible for coordinating the game loop, tactile inputs,
/// rendering the board, and running interactive gameplay components.
class NexusGame extends FlameGame {
  @override
  Future<void> onLoad() async {
    super.onLoad();
    // Initial load setup. Gameplay components will be registered here.
  }
}
